# Python
__pycache__/
*.pyc
.venv/

# Runtime data — real student/faculty PII, biometric embeddings, secrets.
# Never commit these; transfer via USB/local network instead of git.
data/attendance.db
data/attendance.db-wal
data/attendance.db-shm
data/.secret_key
data/enrollment_photos/

# Logs
*.log

# Editor/tooling
.claude/

# Packaged desktop build (software/) — build artifacts + staged ~150MB
# model weights, not source. Rebuild locally via software/build.bat.
software/_build/
software/dist/
